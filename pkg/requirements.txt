# Profile Management API Dependencies
# Minimal set for CRUD operations, no ML/AI dependencies

# Core FastAPI stack
fastapi==0.115.12
uvicorn[standard]==0.33.0
pydantic==2.10.6
pydantic_settings==2.9.1

# Database
pymongo==4.10.1

# Enterprise MongoDB client (Netskope internal only) 
# nsmongo>=25.86.19  # Commented out for local development - will be uncommented in CI/CD
# Note: External builds will automatically fall back to PyMongo via smart routing

# Network and communication
urllib3==2.5.0  # Fix compatibility warning with LibreSSL 2.8.3
grpcio==1.65.5
httpx==0.27.2  # For external service integration (RIS, CfgPusher)
aiohttp==3.11.11  # For health check HTTP requests to external services

# Development and testing (can be removed in production)
pytest==8.3.5
pytest-asyncio==1.0.0
pytest-cov==6.2.1

# Note: ML dependencies (torch, transformers, scikit-learn, onnxruntime) 
# are NOT needed for Profile Management API - removed to reduce image size