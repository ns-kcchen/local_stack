apiVersion: v1
kind: Service
metadata:
  name: mock-ris-service
  namespace: local-stack
  labels:
    app: mock-ris
spec:
  type: NodePort
  selector:
    app: mock-ris
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30082
