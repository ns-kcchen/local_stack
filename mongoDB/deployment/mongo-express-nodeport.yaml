apiVersion: v1
kind: Service
metadata:
  name: mongo-express-nodeport
  namespace: local-stack
spec:
  type: NodePort
  selector:
    app: mongo-express
  ports:
  - name: mongo-express-port
    protocol: TCP
    nodePort: 30081
    port: 8081
    targetPort: 8081
